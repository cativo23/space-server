# Umami stack secrets — copy to `.env` on the host and fill in.
# `.env` is gitignored; never commit real values.

# Postgres password for the umami DB (openssl rand -hex 24)
UMAMI_DB_PASSWORD=

# Umami app secret used to sign/hash (openssl rand -hex 32)
UMAMI_APP_SECRET=
