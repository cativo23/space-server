# Port 143 is blocked externally via UFW - only internal Docker traffic allowed
# ssl=yes allows TLS but doesn't require it (needed for Roundcube internal connection)
disable_plaintext_auth = no
ssl = yes
