version: '3.9'

services:
  mail:
    image: ghcr.io/docker-mailserver/docker-mailserver:latest
    container_name: mail
    hostname: mail
    domainname: ${MAIL_DOMAIN}
    ports:
      - "25:25"      # SMTP
      - "465:465"    # SMTPS (implicit TLS)
      - "587:587"    # Submission (STARTTLS)
      - "993:993"    # IMAPS
      - "995:995"    # POP3S (optional)
      - "143:143"    # IMAP (optional, for testing)
    volumes:
      - mail-data:/var/mail
      - mail-state:/var/mail-state
      - mail-logs:/var/log/mail
      - mail-config:/tmp/docker-mailserver
      - /etc/localtime:/etc/localtime:ro
      - mail-data:/var/mail
      - mail-state:/var/mail-state
      - mail-logs:/var/log/mail
      - mail-config:/tmp/docker-mailserver
      - /etc/localtime:/etc/localtime:ro
    environment:
      # Hostname & Domain
      - OVERRIDES_HOSTNAME=${MAIL_HOSTNAME:-mail}.${MAIL_DOMAIN}
      - OVERRIDES_DOMAINNAME=${MAIL_DOMAIN}

      # Accounts (format: user|password|uid:gid)
      - OVERRIDES_ACCOUNTS=${MAIL_ACCOUNTS}

      # Security - Let mailserver generate self-signed cert (Traefik handles public TLS)
      - PERMIT_DOCKER=network
      - ENABLE_FAIL2BAN=1
      - FAIL2BAN_BLOCKTYPE=drop

      # Spam protection
      - ENABLE_SPAMASSASSIN=1
      - SPAMASSASSIN_SPAM_TO_INBOX=1
      - ENABLE_CLAMAV=1

      # Postfix
      - POSTFIX_MESSAGE_SIZE_LIMIT=${POSTFIX_MESSAGE_SIZE_LIMIT:-52428800}  # 50MB

      # Dovecot
      - ENABLE_MANAGESIEVE=1

      # Logging
      - LOG_LEVEL=info

      # Traefik network (for webmail access)
      - NETWORK_TRAEFIK=web
    networks:
      - web
    cap_add:
      - NET_ADMIN
      - SYS_PTRACE
    restart: always
    healthcheck:
      test: ["CMD", "ss", "--all", "--listen", "|", "grep", "-E", ":(25|465|587|993)"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

  # Optional: Roundcube Webmail
  webmail:
    image: roundcube/roundcubemail:latest
    container_name: webmail
    environment:
      - ROUNDCUBEMAIL_DEFAULT_HOST=ssl://mail
      - ROUNDCUBEMAIL_DEFAULT_PORT=993
      - ROUNDCUBEMAIL_SMTP_SERVER=ssl://mail
      - ROUNDCUBEMAIL_SMTP_PORT=465
      - ROUNDCUBEMAIL_SKIN=elastic
      - ROUNDCUBEMAIL_PLUGINS=archive,zipdownload,managesieve
    networks:
      - web
    volumes:
      - webmail-data:/var/roundcube/db
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.mail.rule=Host(`mail.${MAIL_DOMAIN}`)"
      - "traefik.http.routers.mail.entrypoints=websecure"
      - "traefik.http.routers.mail.tls.certresolver=letsencryptresolver"
      - "traefik.http.routers.mail.middlewares=security-headers@file"
      - "traefik.http.services.mail.loadbalancer.server.port=80"
    depends_on:
      - mail
    restart: always

networks:
  web:
    external: true

volumes:
  mail-data:
  mail-state:
  mail-logs:
  mail-config:
  webmail-data:
